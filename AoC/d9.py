class Person:
    def __init__(self,name_str):
        self.name = name_str
        self.money = 0
    def send(amount):
        self.money = self.money - amount

    def recieve(amount):
        self.money = self.money + amount

    
# for all lines
    # append all users in a list
# make the list unique
# make an object for each user stating with 0 money
# for each transaction:
    p1.send(amount)
    p2.recieve(amount)
